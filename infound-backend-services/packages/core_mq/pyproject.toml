[project]
name = "core-mq"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Thomas", email = "tanher@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "aio-pika>=9.6.1",
    "core-base",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[tool.uv.sources]
core-base = { workspace = true }
